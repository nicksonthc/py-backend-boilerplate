# AWS ECR Configuration Example
# Copy this file to .env.aws and update with your actual values
# Usage: source .env.aws before running build_push_aws.sh

# AWS Region (default: ap-southeast-1)
export AWS_REGION=ap-southeast-1

# ECR Registry URL (replace with your AWS account ID and region)
# Format: {account-id}.dkr.ecr.{region}.amazonaws.com
export ECR_REGISTRY=123456789012.dkr.ecr.ap-southeast-1.amazonaws.com

# ECR Repository name (default: py-backend)
export ECR_REPOSITORY=py-backend

# Optional: AWS credentials (if not using AWS CLI profile)
# Uncomment and set if you're not using AWS CLI configure
# export AWS_ACCESS_KEY_ID=your_access_key_here
# export AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Instructions:
# 1. Copy this file: cp .env.aws.example .env.aws
# 2. Update ECR_REGISTRY with your actual AWS account ID
# 3. Optionally update AWS_REGION and ECR_REPOSITORY
# 4. Source the file: source .env.aws
# 5. Run the script: ./scripts/build_push_aws.sh